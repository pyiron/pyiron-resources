# Resources for pyiron 
This repository contains shell scripts to link to the executables as well as parameter files. 

Install the resources from conda using:
```
conda install -c conda-forge pyiron-data
```
